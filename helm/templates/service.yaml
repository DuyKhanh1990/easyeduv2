apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-easyedu.fullname" . }}
  labels:
    {{- include "new-easyedu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new-easyedu.selectorLabels" . | nindent 4 }}
