apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "new-easyedu.fullname" . }}-config
  labels:
    {{- include "new-easyedu.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
